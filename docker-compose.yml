services:
  bot:
    build:
      context: .
      args:
        HF_TOKEN: ${HF_TOKEN}
    restart: unless-stopped
    env_file: .env
    volumes:
      - bot_data:/app/data
      - hf_cache:/root/.cache/huggingface

volumes:
  bot_data:
  hf_cache:
